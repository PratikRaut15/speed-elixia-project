-- Insert SQL here.

ALTER TABLE `vehicle` ADD `additional_amount` INT NOT NULL ;
-- Successful. Add the Patch to the Applied Patches table.

 INSERT INTO `dbpatches` ( `patchid`, `patchdate`, `appliedby`, `patchdesc` ) 
 VALUES ( 109, NOW(), 'Sanket Sheth','Additional Amount');
