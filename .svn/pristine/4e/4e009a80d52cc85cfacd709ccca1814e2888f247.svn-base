-- Insert SQL here.

ALTER TABLE  `geotest` ADD  `customerno` INT( 11 ) NOT NULL;



-- Successful. Add the Patch to the Applied Patches table.

 INSERT INTO `dbpatches` ( `patchid`, `patchdate`, `appliedby`, `patchdesc` ) 
 VALUES ( 53, NOW(), 'Ajay Tripathi','customerno field added in geotest');
